# Example environment variables (none required by the current codebase)
# If you add API keys, put them here and copy to .env in development.
# VITE_ prefixed variables are exposed to client-side code.
# Example:
# VITE_API_URL=https://api.example.com
